rhoai:
  enabled: true
  namespace: redhat-ods-operator
  channel: stable-3.4
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  # Pin to 3.4.0 to avoid 3.4.1 regressions (AUTH_FAILURE on /v1/models,
  # Perses TLS crashloop, Prometheus missing secret, Gateway OOM).
  # Remove startingCSV and set installPlanApproval: Automatic to upgrade.
  startingCSV: rhods-operator.3.4.0
  installPlanApproval: Manual

connectivityLink:
  enabled: true
  # Dedicated namespace avoids InstallPlan Manual approval contamination
  # from servicemeshoperator3 in openshift-operators
  namespace: redhat-connectivity-link
  channel: stable
  package: rhcl-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace

kuadrant:
  # Namespace for Kuadrant CR and Limitador (not the operator subscription)
  namespace: kuadrant-system

certManager:
  enabled: false

leaderWorkerSet:
  enabled: true
  namespace: leader-worker-set
  channel: stable-v1.0
  source: redhat-operators
  sourceNamespace: openshift-marketplace
