{{- if and .Values.benchmarks.enabled .Values.benchmarks.job.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: benchmarks-{{ .Values.benchmarks.benchmark.profile }}-{{ now | date "20060102-150405" }}
  namespace: {{ include "evaluation.namespace" . }}
  labels:
    {{- include "evaluation.labels" . | nindent 4 }}
    app.kubernetes.io/component: benchmarks
    benchmarks.rhoai/profile: {{ .Values.benchmarks.benchmark.profile }}
spec:
  backoffLimit: {{ .Values.benchmarks.job.backoffLimit }}
  ttlSecondsAfterFinished: {{ .Values.benchmarks.job.ttlSecondsAfterFinished }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: benchmarks
        benchmarks.rhoai/profile: {{ .Values.benchmarks.benchmark.profile }}
    spec:
      serviceAccountName: benchmarks-runner
      restartPolicy: Never
      containers:
        - name: guidellm
          image: {{ .Values.benchmarks.job.image }}
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c"]
          args:
            - |
              {{- if or .Values.benchmarks.benchmark.authToken .Values.benchmarks.benchmark.authSecret }}
              BACKEND_KWARGS="{\"api_key\": \"${AUTH_TOKEN}\"}"
              {{- else }}
              BACKEND_KWARGS="{}"
              {{- end }}
              exec guidellm benchmark \
                --target {{ .Values.benchmarks.benchmark.target | quote }} \
                --model {{ .Values.benchmarks.benchmark.model | quote }} \
                --request-type {{ .Values.benchmarks.benchmark.requestType | quote }} \
                --rate-type {{ .Values.benchmarks.benchmark.profile | quote }} \
                {{- range .Values.benchmarks.benchmark.rates }}
                --rate {{ . | quote }} \
                {{- end }}
                --data '{"prompt_tokens": {{ .Values.benchmarks.benchmark.promptTokens }}, "output_tokens": {{ .Values.benchmarks.benchmark.outputTokens }}}' \
                --processor {{ .Values.benchmarks.benchmark.processor | quote }} \
                --max-seconds {{ .Values.benchmarks.benchmark.maxSeconds | quote }} \
                --outputs {{ .Values.benchmarks.benchmark.outputs | quote }} \
                --output-dir /results \
                --warmup {{ .Values.benchmarks.benchmark.warmup | quote }} \
                --cooldown {{ .Values.benchmarks.benchmark.cooldown | quote }} \
                --backend-kwargs "$BACKEND_KWARGS" \
                {{- range .Values.benchmarks.benchmark.extraArgs }}
                {{ . | quote }} \
                {{- end }}
                --disable-console
          env:
            - name: SSL_CERT_FILE
              value: /etc/pki/tls/certs/ca-bundle.crt
            {{- if or .Values.benchmarks.benchmark.authToken .Values.benchmarks.benchmark.authSecret }}
            - name: AUTH_TOKEN
              {{- if .Values.benchmarks.benchmark.authSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.benchmarks.benchmark.authSecret }}
                  key: token
              {{- else }}
              value: {{ .Values.benchmarks.benchmark.authToken | quote }}
              {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.benchmarks.job.resources | nindent 12 }}
          volumeMounts:
            - name: results
              mountPath: /results
            - name: ca-bundle
              mountPath: /etc/pki/tls/certs/ca-bundle.crt
              subPath: ca-bundle.crt
              readOnly: true
      volumes:
        - name: results
          persistentVolumeClaim:
            claimName: benchmarks-results
        - name: ca-bundle
          configMap:
            name: benchmarks-ca-bundle
{{- end }}
