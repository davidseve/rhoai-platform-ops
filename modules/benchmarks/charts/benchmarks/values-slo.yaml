# SLO validation scenario: constant RPS, verify PrometheusRules don't fire
# Usage: make run-benchmark BENCHMARK_SCENARIO=slo

job:
  enabled: true

benchmark:
  profile: "constant"
  rates:
    - "4"
  maxSeconds: 120
  promptTokens: 32
  outputTokens: 64
