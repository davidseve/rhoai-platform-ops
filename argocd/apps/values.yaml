repoURL: https://github.com/davidseve/rhoai-platform-ops.git
targetRevision: test/coo-rhoai-341
# Cluster domain (e.g. apps.ocp.sandbox1476.opentlc.com).
# Auto-detected by Makefile; sets gateway.hostname and route.host to maas.<clusterDomain>.
clusterDomain: ""

modules:
  database:
    enabled: true
  maas:
    enabled: true
    modelRegistry:
      enabled: false
    models:
      tinyllama-test:
        enabled: true
      granite-2b:
        enabled: true
  observability:
    enabled: true
  evaluation:
    enabled: true
