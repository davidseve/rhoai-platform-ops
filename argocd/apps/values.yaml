repoURL: https://github.com/davidseve/rhoai-platform-ops.git
targetRevision: main
# Cluster domain (e.g. apps.ocp.sandbox1476.opentlc.com).
# Auto-detected by Makefile; sets gateway.hostname and route.host to maas.<clusterDomain>.
clusterDomain: ""

modules:
  maas:
    enabled: true
    models:
      tinyllama-test:
        enabled: true
      tinyllama-fast:
        enabled: true
  observability:
    enabled: true
  benchmarks:
    enabled: true
  evaluation:
    enabled: false
